# DeskTube 작업 내역

## 최근 변경
- 2026-07-19: **정보 화면 개발자 표기 제거 (FR-11 축소)** — 사용자 요청("버전 옆 개발자 삭제"). resw `About_InfoLineFormat` ko/en에서 "· 개발자: DeskTube Dev" 부분 삭제(버전만 표시), AboutViewModel·AboutPage.xaml 주석 동기화, PRD FR-11 문구·변경 이력 갱신(NFR-4의 시작메뉴 게시자명은 무관·유지). README는 개발자 표기 서술이 없어 미변경. **검증**: `dotnet build DeskTube.slnx -p:Platform=x64` 경고 0·오류 0 — 화면 표시는 HUMAN-VERIFY.
- 2026-07-18: **코드 리뷰(/code-review high) 지적 10건 전수 수정** — 브랜치 `task/playlist-stop-toggle` (8각도 파인더 + 후보별 검증 에이전트, CONFIRMED 9 + PLAUSIBLE 1, 사용자 "수정" 승인)
  - **시작 감시 오탐·회귀 3건 (player.html, rev 4→5)**: ① PLAYING 도달 시 해제 가드 제거로 느린 시작(8초 시점 currentTime<1.0) 정상 곡이 -3 오탐 스킵 → **진행 재확인 유예** 도입(만료 시점에 미진행이라도 직전 대비 늘고 있으면 4초 간격 최대 3회 재확인 후 판정) + 1초 시각 보고 인터벌이 진행(≥1.0s) 확인 즉시 감시 해제(정상 곡은 타이머 발화·diag 로그 자체가 없어짐 — 곡·모니터당 diag 누적 소음 제거) ② `time.toFixed(2)`가 getCurrentTime() undefined에서 TypeError로 콜백을 죽여 -3 미발송(죽은 곡에서 영구 정지) → 타입 가드로 -1 정규화, 못 읽는 상태는 죽은 플레이어 증거로 즉시 -3 ③ 절전(suspend) 레이스로 살아남은 스테일 타이머가 정책 재개 직후 오발화 가능 → 'play' 명령에서 감시 재장전.
  - **전 항목 재생 불가 정지 무력화 (PlaybackCoordinator)**: PLAYING 도달이 `_failedItemIds`를 비워 "PLAYING 후 미진행(-3)" 곡만 있는 리스트에서 AllItemsFailed 정지가 영영 발동하지 않고 8초 간격 무한 스킵 루프 → 집합 초기화를 OnPlayerTime의 **실제 진행 확인(masterTime≥1.0)** 시점으로 이동(`PlaybackProgressMinSeconds` 상수 — player.html PLAYBACK_PROGRESS_MIN과 동일 기준). 회귀 테스트 2개 신설(전 곡 Playing 후 미진행 정지 / 진행 확인 시 집합 초기화).
  - **플레이어 재생성 경로 2건 (PlaybackCoordinator)**: ① 무조건 Detach→Attach가 단일 모니터에서 매 복구마다 바탕화면 번쩍임 + Attach 실패 시 플레이어 0개인 채 Playing 영구 지속(EnsureHealthy는 표면 0개를 정상으로 봄) → **기존 표면 위 1차 재생성, 실패 시에만 배경창 재생성 후 2차** + 포기 시 플레이어 0개면 정지(`HandleRecreateGiveUpAsync`) ② 재생성 await 중 모니터 변경 처리가 같은 모니터에 두 번째 플레이어를 만들어 미해제 이중 재생 → `_recreatingMonitors` 재진입 가드.
  - **곡 전환 브리지 fit 미갱신 (player.html)**: 브리지 표시 중 크기 모드 변경 시 썸네일이 이전 objectFit 유지 → `bridgeFit()` 헬퍼 추출, 'fit' 명령에서 브리지도 즉시 갱신.
  - **종료 마커 오판 (App.xaml.cs)**: '앱 종료' 마커가 트레이 종료에만 기록돼 OS 종료·로그오프가 매번 크래시로 오판 → `ProcessExit` 마커 추가(정상 프로세스 종료 커버리지 확대) + 판별 주석을 "단정하지 않음"으로 보정.
  - **정리 2건**: 3진입점(전체듣기/셔플듣기/행 재생)의 동일 정지 가드 복붙 → `TryStopIfPlayingAsync` 공통 헬퍼 / notes.md 30,000자 초과분 아카이브(52,615→12,979자, 57~180행+RCW 항목을 `notes-archive/2026-07.md`로 이동, 인덱스 키워드 갱신).
  - **미수정 잔여분 후속 수정(사용자 "미수정 다시확인" → "수정" 승인)**: ① `RowPlayGlyph` 삭제 — 바이트 동일한 `PlayToggleGlyph`로 통합(매개변수 `playing`으로 일반화, 행 XAML 바인딩 교체) ② `UpdateModePlaying` 무인자화 — "선택 리스트 == 재생 중 리스트" 술어를 호출부 2곳에서 각자 계산하던 것을 함수 내부에서 정본 직접 판정으로 통합(둘 다 null일 때 true가 되는 함정 때문에 null 가드 보존 주석 명시) ③ LoginWindow 초기화 `await` 후 `_isClosed` 재확인 — 결함은 아니나(Close 후 CoreWebView2가 null 반환 → 기존 catch 흡수, 리뷰 REFUTED 판정 유지) 초기화 중 창을 닫으면 "로그인 창 초기화 실패: NullReferenceException" 로그가 남아 정상 취소가 실패처럼 보이던 오해 소지 제거.
  - **검증**: `dotnet build DeskTube.slnx -c Debug -p:Platform=x64` 경고 0·오류 0 / `dotnet test -p:Platform=x64` 127/127(신규 2 포함) / `dotnet format --verify-no-changes` 위반 0 (후속 수정 반영 후 재실행 포함). **HUMAN-VERIFY**: ① 느린 네트워크에서 정상 곡이 스킵되지 않는지 ② 권한 필요 영상만 있는 리스트가 정지+안내로 끝나는지 ③ 재생 중 크기 모드 변경 시 곡 전환 썸네일 맞춤 일치 ④ 브라우저 프로세스 강제 종료 복구 시 바탕화면 번쩍임 없음 ⑤ 행 재생 버튼 정지 아이콘 표시(글리프 통합 후 동일 표시).
  - **무엇을**: `player.html` 단일 파일 — `#stage`에 `img#bridge` 추가. `load` 명령 수신 즉시 해당 곡 썸네일(`i.ytimg.com/vi/{id}/maxresdefault.jpg`)을 표시하고, `PLAYING` 진입 시 0.4s 페이드아웃. maxres 미제공 영상은 120px 자리표시자 판별(naturalWidth≤120)로 `hqdefault` 폴백, 이중 실패 시 숨김(기존 검은 화면과 동일). object-fit은 크기 모드 연동(채움=cover/맞춤=contain/늘리기=fill). `PLAYER_REV` 3→4. C# 무변경(videoId는 load 명령에 기존 포함).
  - **왜**: `loadVideoById`는 이전 영상을 즉시 내리고 다음 영상을 받아오는 1~3초간 검은 화면 — 유튜브 IFrame API는 preload 미지원이라 공백 자체는 제거 불가, 시각적으로 앨범아트 전환으로 대체. 무공백(듀얼 플레이어)은 대공사라 보류.
  - **검증**: `node --check` JS 문법 OK / Playwright 실브라우저(shim 사본): load 직후 visible+maxres(1280x720) 로드+fit=cover → PLAYING 진입 후 visible=false·opacity 0 확인, 곡 전환 2회째도 정상 / 빌드 경고 0·오류 0 / 테스트 125/125 / AppX 배포(rev 4 로드 확인)·앱 재실행. **HUMAN-VERIFY**: 곡이 끝나고 다음 곡으로 넘어갈 때 검은 화면 대신 썸네일 → 재생 시작 시 부드럽게 사라짐.
- 2026-07-18: **"재생 안 됨" 진짜 근본 원인 확정 + 수정 — WebView2 브라우저 프로세스 재시작 레이스 (systematic-debugging 4차, 계측 로그로 재현 포착)** — 로그: `docs/plans/2026-07-18-debug-gimme-dat-love-no-sound.md`
  - **재현(20:45:53)**: 정지→즉시 재생 시 `곡 로드→준비→Unstarted→Buffering` 후 완전 침묵(Playing·오류·pause·start-watchdog 진단 전부 없음 = JS까지 정지). 실측: 앱은 생존·응답인데 **DeskTube 소속 WebView2 프로세스 0개**, 크래시 덤프·이벤트 로그 없음(깨끗한 소멸), ProcessFailed 미발화. 20:49:56 사용자 클릭이 `play → COMException 0x8007139F 전송 실패`로 **좀비 상태를 로그로 직접 증명**(19:45 24연발도 동일 상태 소급 확인).
  - **메커니즘**: StartAsync가 StopAsync 선행 → 이전 플레이어 Dispose로 웹뷰 0개가 되는 순간 공유 브라우저 프로세스 종료 수순 → 직후 캐시된 환경으로 만든 새 컨트롤러가 죽어가는 브라우저에 부착 → 잠시 동작 후 동반 소멸(이벤트 무발화). 타이밍 의존 간헐 재현 — 하루 종일의 "안 됨"(첫 자동재생은 항상 정상, 빠른 재시작 직후만 실패)과 완전 정합.
  - **수정**: `PlayerHost`에 `CoreWebView2Environment.BrowserProcessExited` 안전망 — 초기화 시 브라우저 PID 기록·로그, 소멸 이벤트가 **내 PID와 일치 + 미폐기**일 때만 감지 로그 + `-2` 발화 → 기존 재생성 경로(RecreatePlayerAsync→ResumeCurrentTrack)로 자동 복구. 이전 브라우저의 정상 종료는 PID 불일치로 무시, Dispose에서 구독 해제.
  - **검증**: 빌드 경고 0·오류 0 / 테스트 125/125 / AppX 반영·재실행 후 Bansanka 정상 재생 진입(Playing, 음소거 해제). **HUMAN-VERIFY**: 빠른 정지→재생 반복에도 무음·검정 없이 재생 유지(레이스 시 "소멸 감지→재생성" 로그 후 수 초 내 복구).
  - **5차 (됨/안 됨 교대 보고 → 복구 경로 2건 수정)**: 로그로 교대 메커니즘 확정 — 정지 직후 재생은 **직전과 같은 브라우저 PID에 부착**(종료 예약된 브라우저) 후 1~2초 뒤 동반 소멸("안 됨"), 다음 시도는 새 브라우저라 정상("됨"). 안전망 복구는 ① 기존 배경창 HWND 무효로 `0x80070578` 재생성 실패 ② 별개 플레이버로 초기화 즉시 실패 `0x80004004(E_ABORT)` 때문에 불발. **수정**: RecreatePlayerAsync가 배경창부터 재생성(Detach→Attach) + CreatePlayerAsync 초기화 실패 시 1.5초 후 1회 재시도. **실전 검증**: 재생 중 브라우저 강제 종료 → 캐스케이드 소멸(2연쇄) 포함 ~4초 만에 자가 복구(Playing 재진입) 로그 확인. 빌드 0/0·테스트 125/125·AppX 반영. 잔여 개선 후보(Deferred): 상시 keepalive 웹뷰로 레이스 원천 제거(유휴 메모리 상시 점유 트레이드오프 — 사용자 결정).
- 2026-07-18: **재생 수명주기 진단 로그 계측 (사용자 요청 — "상세 로그 남기고 재테스트")** — "안 됨" 조사에서 드러난 로그 맹점 5곳을 메움. 동작 변경 없음, 로그만 추가.
  - **무엇을**: ① `PlaybackCoordinator` — StartAsync(리스트명·항목수·모드·**음소거·볼륨**), StopAsync("재생 정지"), Pause/Resume("(사용자)" 표기), **PolicyPause/PolicyResume(정책 경로 최초 로그화** — 수행/무시 여부 포함), LoadAll("곡 로드: 제목 (videoId)" — **어느 곡인지 최초 식별 가능**) ② `PowerPolicyService.SetSignal` — 신호(전체화면/배터리/잠금) 변화 시에만 로그(설정 반영 여부 포함, 2초 폴링 스팸 없음) ③ `PlayerHost` — load(videoId)/play/pause 명령 로그(pause는 시작 감시 취소 시점 추적용), `state` 수신 로그(상태 전이) ④ `App` — "=== 앱 시작 ===" / 트레이 종료 "=== 앱 종료 ===" 마커(**종료 마커 없이 끝난 세션 = 비정상 종료 판별**)
  - **왜**: 3차례 "안 됨" 보고 모두 로그로 원인 확정 불가 — 곡 식별 불가·정책 일시정지 무로그·시작/정지 주체 불명·크래시와 정상 종료 구분 불가. 특히 20:26:16 사례는 로드 후 start-watchdog 진단 실종(8초 내 pause 또는 종료)인데 어느 쪽인지 판별 수단이 없었다.
  - **검증**: `dotnet build -c Debug -p:Platform=x64` 경고 0·오류 0 / 테스트 125/125. 새 DeskTube.dll을 AppX 레이아웃(등록된 개발 패키지)에 반영 — 앱 재실행만으로 새 로그 활성.
- 2026-07-18: **FFjDEkZg-54 (i-dle 'Gimme Dat Love' MV) "재생 안 됨/배경 미표시" 보고 조사 — 코드 결함 아님 (systematic-debugging, 수정 없음)** — 로그: `docs/plans/2026-07-18-debug-gimme-dat-love-no-sound.md`
  - **증상**: 이 곡만 앱에서 재생 안 됨 + 배경에 영상 표시 자체가 안 됨(웹은 정상, 다른 곡 정상, 웹에서 한 번 재생 후 앱 재시도하면 표시됨) 보고.
  - **판정 (실행 중 앱 CDP 실측으로 확정)**: WebView2 로더 레지스트리로 원격 디버깅을 붙여(진단 후 원복) 실측 — **앱의 실제 로그인 프로필에서 문제 영상 디코드·렌더·배경 표시 전부 정상**(1920x1080 24fps, dropped 0, 광고/오류 DOM 없음, CDP 스크린샷·배경창 PrintWindow로 표시 확인). 오류 수신 0건, 시작 감시 진단 6건 전부 정상 진행(time=7.3~7.6). 익명 임베드 재현도 정상. 창 계층(Progman 아래 WallpaperSurface·렌더러 체인)도 정상.
  - **"안 됨" 인지의 실체 (복합)**: ① `settings.json` **`IsMuted: true`**(무음) ② WebAudio RMS 실측 — **MV 도입부 ~11초가 사실상 무음 + 어두운 페이드인**(본론 대비 ≈-24dB) ③ **사용자 Windows 바탕화면이 원래 검정**(이미지 없음·배경색 0 0 0)이라 정지/로드 초기/어두운 도입부가 전부 "미표시"로 보임 ④ 로그상 재시도 간격 4~10초 → 매번 0초부터 재시작해 밝은 장면·음악(11초~)에 도달 전 재시작 반복. 다른 곡(Topic 앨범아트 등)은 즉시 밝은 화면·소리라 "다른 건 된다"와 정합. 웹 재생 후 되는 것은 대기 시간 차이로 설명(계정 상태 인과는 완전 배제 불가 — 재발 시 15초 대기 판정 절차를 debug 문서에 기록).
  - **조치**: 코드 무수정. 사용자 안내 — 음소거 해제 + 15초 대기. 진단용 레지스트리 오버라이드 원복 완료(실행 중 인스턴스만 재시작 전까지 localhost:9222 유지).
  - **추가 사례(3차 보고, 20:26:16 HANRORO cgJAmkE_j7U)**: 정적 앨범아트 음원도 "안 됨" 보고 → settings.json(20:26:16 저장)에 **`IsMuted: True` + `LastItemId`=해당 곡** 확정 — 음소거 상태로 재생 시작(무음 확정). 단 start-watchdog 진단 미발화(로드 후 8초 내 pause 또는 프로세스 소멸)에 대해 **사용자가 "8초 내 안 닫았다" 확인** → 정책 일시정지 또는 비정상 종료 의심 잔존(20:20~35 이벤트 로그 크래시 기록은 없음). 당시 로그로는 판별 불가 → 진단 계측 추가로 대응(위 항목). — 사용자 크래시 스택 보고, 로그: `docs/plans/2026-07-18-debug-policypause-issuspended.md`
  - **증상**: `PowerPolicyService` DispatcherQueueTimer → `PolicyPause` → `PauseAll` → `PlayerHost.PostCommand`(197줄)에서 `CoreWebView2.get_IsSuspended`가 COMException 0x8007139F(리소스 상태 부정확) → 타이머 콜백 미처리 전파로 앱 즉사.
  - **근본 원인**: WebView2 프로세스 무효 창에선 COM 호출이 getter 포함 전부 0x8007139F를 던지는데, 커밋 `7ae0005`의 `TryInteract` 흡수 가드가 `PostCommand`에서 `PostWebMessageAsJson`만 감싸고 **`IsSuspended` 조회는 가드 밖**에 남았다(가드보다 먼저 실행). 스택트레이스가 라인 단위로 확정 — 경량 경로(1-A + Phase 4).
  - **수정**: `PlayerHost.cs` ① `PostCommand` — IsSuspended 조회·ResumeFromSuspend·전송 전체를 `TryInteract("플레이어 명령 전송")` 안으로 이동 ② `Suspend` — 같은 PolicyPause 경로의 동일 부류인 `controller.IsVisible = false` + `TrySuspendCoreAsync`를 `TryInteract("플레이어 절전")`로 감쌈(방어 심층화). `_core?.Reload()` 2곳은 Dispose가 null화해 창이 좁아 보류(로그에 기록).
  - **검증**: 빌드 경고 0·오류 0 / 테스트 125/125 / spec·quality 병렬 리뷰 OK(지적 0). 회귀 테스트 불가(CoreWebView2 sealed — 무효 상태 단위 재현 불능) → **HUMAN-VERIFY**: 장기 실행·절전/정책 일시정지 발화 시 동일 크래시 미재발.
- 2026-07-18: **듣기 버튼 선행 아이콘 재생 중 정지 전환 (T1~T2, FR-18 수정)** — plan: `docs/plans/2026-07-18-leading-stop-glyph.md`, 브랜치 `task/playlist-stop-toggle` (mode-indicator 직후 후속)
  - **무엇을**: ① T1 직전의 "문구 뒤 트레일링 정지 아이콘"을 제거하고, **선행 아이콘 자체가 전환** — 전체듣기 E768→E71A(`PlayToggleGlyph(IsSequentialPlaying)`), 셔플듣기 E8B1→E71A(`ShuffleToggleGlyph(IsShufflePlaying)`). `StopLabel()`(무조건 "정지") 제거 → `StopTip(bool)`(재생 중만 Tray_Stop, 평소 **null 폴백** — 장식 아이콘 복귀) 신설, 선행 아이콘의 접근성 이름·툴팁에 연결. 파일 2개(`PlaylistsPage.xaml/.xaml.cs`)만 — VM·resw 무변경 ② T2 PRD FR-18 서술("문구 뒤 추가 아이콘"→"선행 아이콘 전환")·변경 이력·README 갱신
  - **왜**: 사용자 요청 — "문구 앞에 있는 아이콘을 재생시 정지 아이콘으로 변경". 트레일링 방식(직전 세션 확정)을 즉시 대체
  - **결정**: x:Bind 함수 null 반환 → ToolTip 미표시·AutomationProperties.Name 기본 폴백(WinUI 표준 — 레포 첫 선례) / E71A/E768 매핑이 `RowPlayGlyph`와 2회째 중복 — implement-task 3회 공통화 문턱 미달로 별도 유지(발동 스킬 규약이 글로벌 2회 문턱보다 우선), 3회째 등장 시 공통화를 deferred 대장에 등록
  - **검증**: 빌드 경고 0·오류 0 / 테스트 125/125(F-2 재실행) / format 0 / spec·quality 병렬 리뷰 OK(첫 판). **HUMAN-VERIFY**: ① 셔플 재생 중 셔플듣기 아이콘만 ⏹(문구 유지·트레일링 없음) ② 비셔플 재생 중 전체듣기 아이콘만 ⏹ ③ 정지·다른 리스트 선택 시 🔀/▶ 원복 ④ 재생 중 아이콘 툴팁 "정지"·정지 상태 툴팁 없음
- 2026-07-18: **셔플듣기/전체듣기 모드별 트레일링 정지 아이콘 (T1~T2, FR-18 수정)** — plan: `docs/plans/2026-07-18-playlist-mode-indicator.md`, 브랜치 `task/playlist-stop-toggle` (stop-toggle 직후 후속)
  - **무엇을**: ① T1 직전 작업의 "전체듣기 ↔ 정지 문구 교체"를 폐지하고, **재생을 시작한 모드의 버튼**(셔플=셔플듣기 / 비셔플=전체듣기) 문구 뒤에 정지 아이콘(E71A, 접근성 이름·툴팁 `StopLabel()`=Tray_Stop)을 표시·클릭 시 정지로 교체. `IsSelectedPlaying` 제거 → `IsShufflePlaying`·`IsSequentialPlaying` 신설(+`UpdateModePlaying` private 헬퍼 — UpdateNowPlaying 말미·OnSelectedPlaylistChanged 호출), `ShuffleAllAsync`도 토글화. `PlayToggleGlyph`·`PlaylistsStopLabel.Text`(양 언어)·문구 스왑 제거(고아 정리) ② T2 PRD FR-18 서술을 새 방식으로 수정 + 변경 이력, README 갱신
  - **왜**: 사용자 보고 — 셔플듣기로 재생해도 전체듣기 버튼만 "정지"로 바뀌어 어느 모드로 재생 중인지 구분 불가. 명확화 답변: "정지 문구로만 표시하지 말고 셔플듣기 문구 뒤에 정지 아이콘 추가"
  - **함정·결정**: 모드 정본은 `Coordinator.Settings.Mode` — `SetModeAsync`가 설정·실행 큐를 함께 갱신하고 유일 호출부가 재생 시작 직전이라 재생 중 불변(D2) / 셔플 외 모드(Random·RepeatOne·RepeatAll 레거시 저장값)는 비셔플 묶음으로 전체듣기 쪽 표시(D3) / 활성 모드 버튼=정지, 비활성=그 모드로 전환 재시작(현행 유지, D4) / 리뷰 MINOR: "멜론 plan D4" 주석 표기를 리뷰 2건이 오타로 오인 — 레포 관례 표기라 유지, 명확화 검토를 Deferred 등록
  - **검증**: 빌드 경고 0·오류 0 / 테스트 125/125(F-2 재실행 포함) / format 위반 0 / spec·quality 병렬 리뷰 OK(MINOR 1 동일건). **HUMAN-VERIFY**: ① 셔플듣기 재생 중 셔플듣기 버튼에만 정지 아이콘 → 클릭 시 정지 ② 전체듣기·행 재생 중 전체듣기 버튼에만 아이콘 ③ 정지·다른 리스트 선택 시 아이콘 없음 ④ 아이콘 있는 버튼 클릭=정지, 없는 버튼 클릭=그 모드로 전환
- 2026-07-18: **플레이리스트 재생/정지 토글 (T1~T2, FR-18 보강)** — plan: `docs/plans/2026-07-18-playlist-stop-toggle.md`, 브랜치 `task/playlist-stop-toggle`
  - **무엇을**: ① T1 상단 전체듣기 버튼 토글 — `PlaylistsViewModel.IsSelectedPlaying`(observable, 정본 Coordinator.CurrentPlaylistId의 미러) 신설, `UpdateNowPlaying` 말미·`OnSelectedPlaylistChanged`에서 갱신, `PlayAsync`는 선택 리스트 재생(일시정지 포함) 중이면 `StopAsync` 후 반환. 행 버튼 토글 — `PlayItemAsync`→`TogglePlayItemAsync` 개명(사전 승인, 호출부 `OnPlayItemClick` 1곳 동반 수정), `entry.IsNowPlaying`이면 정지. XAML 상단 버튼 글리프 `PlayToggleGlyph`(E71A/E768) 함수 바인딩 + 문구 `PlayAllLabel`/`PlaylistsStopLabel` x:Uid 가시성 스왑(InvertVisibility 재사용), 행 버튼 `RowPlayGlyph`·접근성 이름 `RowPlayName`(Loc: Tray_Stop/ItemPlayName) 동적화, resw 양 언어 `PlaylistsStopLabel.Text` 신설·`ItemPlayButton.[…]AutomationProperties.Name`→`ItemPlayName` plain 키 이전(값 보존) ② T2 PRD FR-18 토글 취지+검증(HUMAN-VERIFY)+변경 이력 1줄, README "재생/정지 토글" 항목 추가
  - **왜**: 사용자 요청 — 플레이리스트 페이지에 재생 진입점만 있고 정지 수단이 없음. 질문 확정: 선택 리스트 기준(다른 리스트 재생 중이면 전체듣기 표시 → 클릭 시 전환 = 현행 유지), 행 버튼 포함, 일시정지 중에도 정지 표시(홈 정지 pill 선례 통일), 미커밋 진단 계측 7파일은 그대로 유지(스테이징 제외)
  - **함정·결정**: StartAsync가 재생 중 재호출 시 StopAsync 선행 → 시작/정지/전환 모두 StatusChanged 발화 보장이라 기존 UpdateNowPlaying 경로에 토글 갱신을 얹음(신규 구독 불필요) / 선택만 바뀌는 경우는 OnSelectedPlaylistChanged에서 별도 재판정 / Loc의 슬래시 키(x:Uid 속성 키) 조회는 레포 선례가 없어 plain 키 이전으로 우회 / 정지 문구는 Tray_Stop 재사용
  - **검증**: `dotnet build -c Debug -p:Platform=x64` 경고 0·오류 0 / 테스트 125/125(코디네이터 무변경 — 신규 테스트 없음, VM 표시 로직은 레포 관례상 테스트 비대상) / dotnet format 위반 0 / spec·quality 리뷰 병렬 OK / plan-completion-reviewer(Phase F-7)·Phase G는 plan 참조. **HUMAN-VERIFY**: ① 선택 리스트 재생·일시정지 중 상단 버튼 "정지"(E71A) → 클릭 시 정지 ② 정지·다른 리스트 재생 중 "전체듣기" → 클릭 시 선택 리스트 재생 ③ 재생 중인 곡 행 버튼 정지 표시·클릭 시 정지, 그 외 행은 "이 곡부터 듣기" ④ 곡 전환·정지 시 행 표시 이동·복귀
- 2026-07-18: **권한 필요 영상 스킵 미동작 재보고 — 진단 계측 + WebView2 캐시 무력화** (systematic-debugging 3라운드)
  - **증상**: currentTime 판정 수정 후에도 권한 필요 영상에서 스킵 미동작. 로그에 `-3` 수신 기록 전무(코디네이터 `HandlePlayerError`는 무조건 로그하므로 미도착 확정). AppX 배포본은 소스와 동일(디스크는 최신) — 감시 자체가 발화하지 않았거나 실행 중인 스크립트가 옛 버전.
  - **가설 2갈래**: H1) 앱 환경에선 이 영상의 currentTime이 실제로 1초 이상 진행(감시가 설계대로 침묵 — "재생 안 됨"의 실체가 별개) / H2) **WebView2가 가상 호스트 응답(player.html)을 HTTP 캐시로 서빙**(프로필 `LocalFolder\WebView2` 영속) → 새 감시 코드 자체가 실행 안 됨.
  - **조치 (한 번의 테스트로 양쪽 판정)**: ① player.html에 `PLAYER_REV`(=3) 신설, `ready` 메시지에 rev 동봉 → C#이 "플레이어 준비 (player.html rev N)" 로그(실행 중 스크립트 버전 확정 — rev<3이면 H2) ② 시작 감시 만료 시 판정 근거(`state`·`currentTime`)를 `diag` 메시지로 항상 보고 → C# "플레이어 진단:" 로그(H1 판정) ③ **Navigate에 쿼리스트링 캐시 무력화**(`player.html?t={TickCount64}` — 실행마다 새 URL이라 항상 디스크 최신본, 앱 업데이트 후 옛 스크립트 실행 방지 자체가 결함이라 진단과 무관하게 근본 수정) ④ `OnWebMessageReceived`에 `diag` 케이스 추가.
  - **검증**: 빌드 경고 0·오류 0 / 테스트 125/125. **HUMAN-VERIFY(판정)**: 앱 재시작 → 권한 필요 영상 재생 → 로그의 `rev`(3이어야 함)와 `start-watchdog state=.. time=..` 값 확인 — time<1이면 -3 전송·스킵돼야 하고, time≥1이면 "재생 안 됨"의 실체를 다시 정의해야 함.

## 아카이브 인덱스
- 2026-07.md: part1 코어 엔진·part2 UI 통합·배경창 Win32 호스트(AV 크래시 수정)·UI 성능 배치(FR-16 크기 모드 등)·음소거 토글 분리·멜론 차트 스타일(FR-18)·디자인 1a 리스타일·트레이/반복·자동 재생(FR-19)·자막(FR-20)·후원(FR-21)·재생 중 표시·CoreWebView2 RCW 크래시 수정·재생 미진행(currentTime) 판정 전환
