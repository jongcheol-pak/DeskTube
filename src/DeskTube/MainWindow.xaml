<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    x:Class="DeskTube.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winex="using:WinUIEx">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 커스텀 타이틀바 (T5 — ExtendsContentIntoTitleBar, 드래그 영역은 코드에서 SetTitleBar) -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="48">
            <!-- 앱명(브랜드명)은 번역 대상이 아니라 직접 표기 -->
            <TextBlock
                Text="DeskTube"
                Style="{StaticResource CaptionTextBlockStyle}"
                VerticalAlignment="Center"
                Margin="16,0,0,0" />
        </Grid>

        <!-- 설정 셸 — NavigationView (plan D1). 플레이리스트(T3)·정보(T6) 항목은 해당 task에서 추가 -->
        <NavigationView
            x:Name="Nav"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
            SelectionChanged="OnNavSelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="NavHome" x:Name="NavHomeItem" Tag="home" Icon="Home" />
                <NavigationViewItem x:Uid="NavPlaylists" Tag="playlists" Icon="List" />
                <NavigationViewItem x:Uid="NavAbout" Tag="about">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <Frame x:Name="ContentFrame" />
        </NavigationView>
        <!-- 트레이 진입 안내 (재생할 리스트 없음 등 — plan T1 Edge). 셸 위 오버레이 -->
        <InfoBar
            x:Name="NoticeBar"
            Grid.Row="1"
            IsOpen="False"
            Severity="Informational"
            VerticalAlignment="Top"
            HorizontalAlignment="Center"
            MaxWidth="600"
            Margin="16,48,16,0" />
    </Grid>
</winex:WindowEx>
