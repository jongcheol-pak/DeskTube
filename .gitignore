# 빌드 산출물
bin/
obj/
AppPackages/
publish/
BundleArtifacts/

# 사용자별 설정
*.user
*.suo
.vs/

# 테스트/커버리지 산출물
TestResults/
*.trx

# 패키징 서명 (실제 인증서 커밋 금지)
*.pfx
*.cer

# Store 연결 정보 (파트너 센터 계정의 다른 앱 identity 포함 — 공개 저장소 커밋 금지)
Package.StoreAssociation.xml

# 로컬 환경
.env

# Playwright MCP 런타임 로그 (세션별 임시 산출물)
.playwright-mcp/

# 에이전트 작업 가이드 (로컬 전용 — 공개 저장소 미게시)
AGENTS.md
