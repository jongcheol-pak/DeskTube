# DeskTube 작업 내역

## 최근 변경
- 2026-07-18: **재생 중인 곡(항목) 표시 — 우측 항목 목록 (T1~T3, FR-18 보강)** — plan: `docs/plans/2026-07-18-now-playing-item.md`, 브랜치 `task/now-playing-item`
  - **무엇을**: ① T1 `PlaybackCoordinator.CurrentItemId`(Guid?, 정지 시 null) 공개 속성 + `CurrentItemChanged` 공개 이벤트 신설 — `SetCurrentItem`이 ID 변화 시에만 발화(SetStatus 가드 복제), LoadAll(단일 곡 로드 경로)에서 확정·StopAsync에서 해제. StartAsync는 `CurrentPlaylistId`를 LoadAll(이벤트 발화) 앞으로 이동. 테스트 2건(시작·전환·일시정지 무발화·정지 수명주기 + 동일 곡 재로드 무발화) ② T2 우측 항목 — `PlaylistItemEntry.IsNowPlaying` + `UpdateNowPlayingItem`(전 Items에 `entry.Id==CurrentItemId` 반영) + CurrentItemChanged 구독(Populate 멱등 재구독·Detach 해제·TryEnqueue 마셜링) + RefreshItems·Populate 말미 초기 반영, XAML col0에 순위 TextBlock + 스피커 FontIcon(E767·AppAccentBrush) 겹침·`RankVisibility` 정적 헬퍼로 순위 가시성 토글, 접근성 이름·툴팁은 기존 `NowPlayingLabel()` resw 재사용 ③ T3 PRD FR-18 보강(본문·검증방법 CurrentItemId·변경 이력) + README 차트형 목록 설명
  - **왜**: 사용자 요청 — 배경 재생 중인 곡이 우측 항목 목록에서 어느 행인지 시각 표시. 직전 now-playing 작업(2026-07-17)은 **리스트 단위**였고 항목 단위를 Out of Scope로 남긴 후속. 표시 형태(순위 자리 코럴 스피커 E767)·PRD 보강은 질문 라운드 확정
  - **함정·결정**: 같은 리스트 내 곡 전환(AdvanceAsync→LoadAll)은 Status가 Playing→Playing이라 StatusChanged 미발화 → 항목 표시엔 전용 `CurrentItemChanged` 필수(리스트 표시는 StatusChanged로 충분했음) / ReloadCurrentTrack의 동일 곡 재로드는 SetCurrentItem 가드로 무발화(중복 UI 갱신 방지) / StartAsync에서 CurrentItemChanged가 CurrentPlaylistId 확정 전 발화되던 순서 결함(quality 리뷰 M1) → CurrentPlaylistId를 LoadAll 앞으로 이동해 "발화 전 상태 확정" 파일 관례 준수 / col0 순위↔글리프 겹침은 RankVisibility 정적 헬퍼로 상호배타(ActiveWeight 선례) / 항목 ID는 전역 고유 Guid라 다른 리스트 선택 시 자동 불일치 / VM 표시 로직은 레포 관례상 테스트 비대상(서비스 계층만)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 123/123(신규 2: CurrentItemId 수명주기·동일 곡 재로드 무발화 — 기존 121 유지) / dotnet format 위반 0 / T1·T2 spec+quality 이중 리뷰(T1 quality MAJOR 1 — CurrentPlaylistId 발화 순서, 수정 후 재리뷰 OK / T2 첫 판 OK) / F-7·Phase G 통과. **HUMAN-VERIFY 잔여**: ① 재생 시작 → 재생 중 행이 순위 대신 코럴 스피커 ② 곡 전환 시 글리프 다음 행 이동(이전 행 순위 복귀) ③ 정지 전 행 순위 복귀·일시정지 유지 ④ 재생 중 다른 리스트 선택 시 글리프 없음 ⑤ 재생 중 페이지·리스트 진입 즉시 표시 ⑥ 툴팁("재생 중"/"Now playing")
- 2026-07-18: **Microsoft.WindowsAppSDK 2.2.0 → 2.3.1 채택 (의존성)** — 사용자 승인. 이번 세션 빌드/복원이 csproj `PackageReference` 버전을 2.3.1로 자동 갱신(세션 시작 시점엔 미변경 상태였음). 되돌려도 재빌드 시 재발 소지가 있어 2.3.1로 고정 채택. `src/DeskTube/DeskTube.csproj` 1줄 + AGENTS.md 주요 패키지 표기 동기화(2.2→2.3.1, AGENTS는 gitignore 전역 제외라 커밋 미포함·로컬 전용). **검증**: 이번 세션 Release 빌드(`-p:Platform=x64 -c Release`) 경고 0·오류 0 + 테스트 121/121 모두 2.3.1 기준 통과.
- 2026-07-18: **앱 버전 0.1.0.0 → 1.0.0.0 상향 (Store 배포 준비)** — 사용자 요청. `Package.appxmanifest`의 `Identity/@Version` 한 줄만 변경. 첫 Store 제출 관례상 1.0.0.0으로 확정. 정보 화면은 `Package.Current.Id.Version`을 동적으로 읽으므로(AboutViewModel.ResolveVersion) 하드코딩 수정 불필요 — 자동 반영. manifest는 VS 생성 관례대로 UTF-8 BOM 유지(원본도 BOM, CLAUDE.md 인코딩 예외). **검증**: Release 빌드(`-p:Platform=x64`) 경고 0·오류 0(매니페스트 파싱 정상). **배포 잔여(사용자 수행)**: Identity 교체(VS Store 연결 마법사)·개인정보처리방침 URL·WACK 통과·실측 3건·파트너 센터 등록 — Deferred 대장 참조.
- 2026-07-18: **콘텐츠 고정 폭 1000 → 900 축소** — 사용자 요청. `Resources/DesignTokens.xaml`의 `AppPageContentWidth` 토큰 값 하나만 변경(1000→900)해 홈·설정·정보 공통 콘텐츠 폭과 플레이리스트 최소 폭에 일괄 전파. 값을 명시하던 주석 5곳(DesignTokens·SettingsPage·AboutPage·HomePage·PlaylistsPage.xaml + PlaylistsPage.xaml.cs)과 AGENTS.md 디자인 규칙 6번 문구를 900으로 동기 갱신. **검증**: `dotnet build -p:Platform=x64` 경고 0·오류 0. 실제 레이아웃 폭은 사용자 화면 확인 필요(HUMAN-VERIFY).
- 2026-07-18: **한글 리소스 UI 문구 간결화 2건 (humanize)** — `ko-KR/Resources.resw`의 진행형("~하고 있습니다") 2건을 "~중입니다"로 다듬음(`Common_NotReady`, `Playlists_DeletePlayingConfirm`). AI 티 점검 결과 등급 A(수정 불필요)였고, 사용자가 선택한 미세 다듬기만 반영. 커밋 `525677f`.
- 2026-07-18: **AGENTS.md 프로젝트 사실 3건 기록 (record-project-fact)** — Deferred 대장 대기 항목 소진
  - **무엇을**: ① Stack 표기 `.NET 8`/`net8.0-…` → `.NET 10`/`net10.0-windows10.0.22621.0`(csproj `TargetFramework` 실물) ② Build & Test의 Test 명령에 `-c Debug -p:Platform=x64` 추가(미지정 시 MSIX AnyCPU 오류) ③ MSIX 사이드로드 패키징 명령(`MSBuild … GenerateAppxPackageOnBuild=true UapAppxPackageBuildMode=SideloadOnly AppxBundle=Never`) 기록. `docs/plans/deferred.md`의 해당 3개 대기 항목을 종결로 이동
  - **왜**: 사용자 "모두 수정" 요청 — Deferred에 쌓인 AGENTS.md stale/누락 항목 정리. AGENTS.md는 세션 시작 시 자동 로드되므로 재발견(grep) 비용 제거
  - **검증**: 값 교차검증 — TFM은 `src/DeskTube/DeskTube.csproj` 직접 확인, Test 플랫폼 플래그·MSIX 명령은 notes/`docs/verification-2026-07.md`/README의 검증 이력과 일치(추측 아님). 문서 변경이라 빌드 대상 아님
  - **함정 (중요)**: `AGENTS.md`는 `~/.gitignore_global`(5행 `AGENTS.md`)로 **전역 제외** → git 미추적·**커밋 안 됨**. 이 PC에서는 세션 시작 자동 로드로 정상 반영되나 **다른 PC로는 공유되지 않음**. 사용자 확인 결과 "로컬 전용 유지"로 결정(전역 정책 존중). ⇒ AGENTS.md에 사실을 기록해도 PC 간 공유는 안 되므로, 크로스 PC 공유가 필요한 규약은 커밋되는 문서(notes/deferred/README)에 남길 것
- 2026-07-18: **정보 화면 후원 링크 카드 추가 (T1~T2, FR-21 신설)** — plan: `docs/plans/2026-07-18-sponsor-link.md`, 브랜치 `task/sponsor-link`
  - **무엇을**: ① T1 PRD `docs/prd.md`에 FR-21(정보 화면 개발 지원 후원 링크 — GitHub Sponsors, Could) 신설 + 변경 이력 1줄 ② T2 정보(About) 화면 개인정보 카드와 라이선스 제목 사이에 `controls:SettingsCard`(클릭형, HeaderIcon 채운 하트 `EB52`·ActionIcon 외부링크 `E8A7`) 추가, `AboutPage.xaml.cs`에 `OnSupportCardClick` 핸들러 + `SponsorUrl` 상수(`https://github.com/sponsors/jongcheol-pak`) — 기존 `OnLicenseCardClick`의 `Windows.System.Launcher.LaunchUriAsync` + try-catch·`AppLog.Write` 패턴 그대로 복제, `Strings/{ko-KR,en-US}` `SupportCard.Header`/`.Description`(개발 지원 / Support development 등), README 정보 화면 기능 갱신
  - **왜**: 사용자 요청 — 앱에 개발자 후원(기부) 링크 추가. 배치(설정 vs 정보)는 질문 라운드에서 **정보 화면**으로 확정(관례상 후원은 정보 화면, 기존 라이선스 카드 외부 링크 패턴 재사용). 단일 채널·PRD 신규 FR 등록·추천 기본 문구·하트 아이콘 모두 질문 확정. URL은 사용자 제공(공개 Sponsors 페이지라 시크릿 아님)
  - **함정·결정**: 후원 URL은 번역 대상이 아니라 resw가 아닌 code-behind 상수로 둠(라이선스 URL이 index.json 데이터인 것과 대비 — 주석 명시) / 하트 글리프는 plan-reviewer MINOR 반영해 외곽선 `EB51`→채운 `EB52` / 정적 링크·ViewModel 미변경이라 라이선스 카드처럼 code-behind 핸들러로 충분(추상화 없음, YAGNI)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 121/121(회귀 없음, 신규 테스트 없음 — 정적 링크·UI) / dotnet format 위반 0 / T2 spec+quality 이중 리뷰 OK(이슈 0) / plan-reviewer BLOCKER·MAJOR 0(MINOR 1 하트 글리프 반영) / F-7·Phase G 통과. **HUMAN-VERIFY 잔여**: ① 정보 화면에 "개발 지원" 카드 시각 표시(하트·외부링크 아이콘) ② 카드 클릭 → 기본 브라우저에서 GitHub Sponsors 페이지 열림 ③ 언어 전환 시 ko/en 문구
  - **유의사항(비차단)**: 외부 후원 링크는 Microsoft Store에서 일반적으로 허용되나 Store 제출 전 최신 정책 확인 권장(개발·빌드 무관)
- 2026-07-17: **성능 최적화 3건 — 정책 일시정지 절전·볼륨 저장 디바운스·미러 캡 기본 켬 (T1~T4, NFR-1·NFR-2 보강)** — plan: `docs/plans/2026-07-17-perf-optimizations.md`, 브랜치 `task/perf-optimizations`
  - **무엇을**: ① T1 `IPlayerHost.Suspend()/ResumeFromSuspend()` 신설 — PlayerHost가 `IsVisible=false` 후 `TrySuspendAsync`(best-effort, 실패 로그만), 해제는 `IsSuspended` 확인 Resume + `IsVisible=true`, PostCommand가 절전 중이면 자동 해제 후 전송. 코디네이터 `PolicyPause` → PauseAll 후 SuspendAll, `ResumeOrSkipFailed` 초입 ResumeAllFromSuspend(사용자 Resume·PolicyResume 공용 합류점) — 사용자 일시정지는 비절전 ② T2 `SetVolumeAsync` — 플레이어 반영 즉시 + 저장은 CTS 교체·500ms 디바운스(마지막 값 1회), Dispose에서 pending 시 FireAndForget 최종 저장 ③ T3 `AppSettings.ReduceMirrorQuality` 기본 true(신규 설치·필드 미기록만 — 직렬화가 기본값도 기록해 기존 파일은 유지), 테스트 Harness false 고정·왕복 값 반전·기본값 단언 ④ T4 PRD NFR-1(절전)·NFR-2(미러 캡 기본) 보강 + README
  - **왜**: 성능 검토(정적 분석) 후 사용자 "모두 수정" — 정책 일시정지(전체화면·배터리 세이버·잠금)는 장시간 지속되는데 WebView2가 풀 상주(모니터당 수백 MB + 컴포지팅), 볼륨 드래그가 틱마다 settings.json 원자 쓰기, 미러 캡은 다중 모니터 CPU 최대 레버인데 opt-in. 적용 범위(정책 전체·사용자 일시정지 제외)·미러 캡 기본 켬은 질문 라운드 확정
  - **함정·결정**: `TrySuspendAsync`는 `Controller.IsVisible=false` 선행 필수(아니면 COMException — 공식 문서 context7 확인), `IsVisible=true`만으로 자동 재개되지만 명시 Resume을 앞세워 결정적 순서 고정 / 절전 중 배경은 검게 표시(렌더링 중단) — 질문 라운드 고지·수용, 배터리 세이버 중 검은 배경은 절전 취지 부합 / 절전 중 명령(음소거 등)은 PostCommand 자동 해제 후 전송·재절전 안 함(다음 정책 일시정지에서 회복) / Dispose 최종 저장은 FireAndForget 헬퍼 경유(quality 리뷰 MAJOR — 직접 `_ =` discard는 예외 유실) / Harness에 ReduceMirrorQuality=false 고정(기존 "꺼짐→켬" 테스트 의도 보존 — IsMuted 고정 선례) / 왕복 테스트 값은 기본값과 다르게(false로 반전)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 121/121(신규 5: 절전 순서 pause→suspend·resume→play, 사용자 일시정지 비절전, 정책 중 사용자 재개 해제, 볼륨 디바운스 저장 1회·마지막 값, 미러 캡 기본값 단언) / dotnet format 위반 0 / T1~T3 spec+quality 이중 리뷰(T2 quality MAJOR 1 — FireAndForget 수정 후 재리뷰 OK, MINOR 2 follow-up) / plan-reviewer BLOCKER·MAJOR·MINOR 0. **HUMAN-VERIFY 잔여**: ① 재생 중 전체화면 진입 → 작업 관리자 WebView2 CPU ~0·메모리 감소, 해제 → 정상 재개 ② 화면 잠금→해제 동일 ③ 배터리 세이버 중 검은 배경(수용 확인) ④ 정책 일시정지 중 트레이 "재생" 정상 재개 ⑤ 볼륨 드래그 즉시 반영·재시작 후 마지막 값 복원 ⑥ 신규 설치 다중 모니터 → 미러 720 하향
  - **미처리 Deferred**: `_volumeSaveCts` 미Dispose(CancelMetadataBackfill 관례 승계) / 볼륨 디바운스 테스트 실시간 대기(가상 시계 인프라 도입 시 전환)
- 2026-07-17: **현재 재생 중 플레이리스트 표시 (T1~T4, FR-18 보강)** — plan: `docs/plans/2026-07-17-now-playing-indicator.md`, 브랜치 `task/now-playing-indicator`
  - **무엇을**: ① T1 `PlaybackCoordinator.CurrentPlaylistId`(Guid?, 정지 시 null) 공개 속성 신설 — StartAsync가 `SetStatus(Playing)` 직전 설정·StopAsync가 `SetStatus(Stopped)` 직전 해제, `PlaylistsViewModel.IsPlaying` 판정식을 이 속성 비교로 교체(계약 동일) ② T2 플레이리스트 좌측 목록 — `PlaylistEntry.IsNowPlaying` + StatusChanged 구독(TryEnqueue 마셜링·Populate 멱등 재구독·Detach 해제, HomeViewModel 패턴 복제) + 템플릿 4열화(E767 코럴 글리프, AutomationProperties.Name·ToolTip = `NowPlayingLabel()` 정적 헬퍼 → resw `NowPlayingIndicator` ko/en) ③ T3 홈 빠른 재생 칩 — `QuickChip` record→ObservableObject sealed partial 클래스(Id·Name·CountText 불변 + IsNowPlaying), RefreshChips 생성 시 초기값 + UpdatePlaybackState 편승 갱신(신규 구독 없음), 칩 템플릿 동일 글리프 + HomePage `xmlns:local` 신설 ④ T4 PRD FR-18 보강(본문·검증 방법·변경 이력) + README 핵심 기능 1줄
  - **왜**: 사용자 요청 — 재생 중인 리스트가 어느 것인지 두 화면에서 시각 표시. 표시 형태(코럴 스피커 글리프)·PRD 보강은 질문 라운드 확정. 일시정지 중 표시 유지(홈 pill·트레이 선례 `Status != Stopped`)는 자체 확정
  - **함정·결정**: 기존 StartAsync는 `SetStatus(Playing)` **후에** LastPlaylistId 기록 — 이벤트 직결 시 stale 값 레이스(기존 VM들은 TryEnqueue 지연으로 우연 회피). CurrentPlaylistId를 SetStatus 앞에 확정해 근본 제거(테스트가 발화 시점 값 단언) / LastPlaylistId는 정지 후에도 남는 재개용 이력이라 별개 유지 / 접근성 문구는 resw attached 구문 대신 정적 헬퍼 x:Bind(MonitorCardsControl.BadgeToggleName 선례) — NowPlayingLabel은 페이지별 독립 복제 2회(3회 공통화 문턱 미만) / 같은 리스트 재시작 시 글리프 순간 깜빡임은 허용(plan-reviewer m1 — Stop→Start 상태 사실 그대로)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 117/117(신규 1: CurrentPlaylistId 시작·일시정지·정지 수명 주기 + 이벤트 발화 시점 단언) / dotnet format 위반 0 / task별 spec+quality 이중 리뷰 전건 첫 판 OK / plan-reviewer BLOCKER·MAJOR 0(MINOR 1 깜빡임 — 허용 명시). **HUMAN-VERIFY 잔여**: ① 재생 시작 → 플레이리스트 목록·홈 칩 코럴 스피커 표시 ② 정지·전곡 실패·리스트 삭제 시 해제 ③ 일시정지 중 유지 ④ 재생 중 페이지 진입 즉시 표시 ⑤ 같은 리스트 재시작 깜빡임 수준 ⑥ 글리프 툴팁("재생 중"/"Now playing")
- 2026-07-17: **코드 리뷰(/code-review high) 지적 18건 전수 수정** — 브랜치 `task/home-defaults-tray` (8각도 파인더 + 검증 에이전트로 CONFIRMED/PLAUSIBLE 판정된 정확성 9건 + 클린업 9건, 사용자 "모두 수정" 승인)
  - **정확성**: ① 트레이 메뉴 전항목 무동작 회귀 수정 — PopupMenu 모드는 Click 이벤트를 발생시키지 않음(H.NotifyIcon 2.4.1 DLL 디컴파일 실증) → 4개 항목 전부 `Command`(RelayCommand/AsyncRelayCommand) 연결로 전환 ② 전곡 재생 불가 판정 재설계 — 연속 카운터(`_errorSkipCount`) → 서로 다른 항목 집합(`_failedItemIds`) 기준(Random 조기 오탐 제거), RepeatOne은 첫 에러 즉시 정지(같은 곡 Count회 재로드 낭비 제거) ③ 일시정지 중 재생 불가 에러 시 자동 전진 금지(`_advanceAfterResume`) — loadVideoById 즉시 재생이 일시정지(전원 정책 포함)를 깨던 문제, 재개 시 스킵 ④ 빠른 재생 리스트 식별을 지역화 이름 비교 → 안정 ID(`AppSettings.QuickPlaylistId`)로 전환(언어 전환·동명 사용자 리스트 오동작 제거, 구버전 데이터는 이름 폴백 1회) — App·HomeViewModel 양쪽 ⑤ 전곡 불가 정지 안내를 코디네이터 직접 토스트 → `AllItemsFailed` 이벤트로 승격, App이 `ShowMainWindow(문구)` 라우팅(트레이 전용 상태에서도 보임, 코디네이터의 WinUI 의존 제거) ⑥ PlaylistsPage 가로 래퍼 무한 폭 측정 수정 — `MaxWidth=max(뷰포트,1000)` 코드비하인드(긴 제목 말줄임 복원) ⑦ 계정 프로브 실패 시 로그인 버튼 영구 비활성 수정 — 미로그인 간주+버튼 활성(로그인=재시도 경로) ⑧ player.html 자막 재적용 — onStateChange PLAYING 시 applyCaptions()(load 직후 동기 적용이 새 영상 초기화에 덮이는 레이스) ⑨ 홈 URL 복원을 VM 수명당 1회로(지운 입력 재주입 방지)
  - **클린업**: `AccountPanelViewModel.Shared` 단일 공유 인스턴스(페이지별 프로브 중복·기동 경합·상태 불일치 제거 — 프로브는 최초 1회+로그인 창 닫힘/로그아웃, 언어 전환 대비 세션 서비스는 Attach마다 핸들 재생성) / `PlaybackCoordinator.StartLastAsync` 신설(App 자동 시작·트레이 재생 중복 제거) / `InitializePlayer` 헬퍼(플레이어 초기화 4단계 3중 반복 수렴) / 홈 PlayAsync 이중 settings.json 저장 제거(StartAsync 성공 저장에 편승, 실패 시 URL 원복) / 구글 "G" 로고 `Controls/GoogleLogoIcon` 공용 컨트롤 + 브랜드 4색 DesignTokens 토큰화(AGENTS 디자인 규칙 2 정합) / `Views/LoginFlow.Open` 공통 헬퍼(홈·설정 코드비하인드 중복 제거) / `AppPageFrameWidth`(1112 손계산 파생 토큰) 삭제 — 홈 코드비하인드가 원본 토큰 합산 / AGENTS.md 디자인 규칙 6 갱신(MaxWidth 900 → 고정 1000) / HomePage 헤더 주석 현행화
  - **함정·결정**: RepeatOne 정지는 "전곡 불가" 문구를 공유(다음 곡도 같은 곡이라 재시도 무의미 — 별도 문구는 과설계) / QuickPlaylistId는 StartAsync 성공 저장에 편승해 영속(실패 시에도 in-memory 유지 — 값 자체는 항상 정확) / 공유 AccountPanel로 페이지 진입 시 세션 만료 자동 감지는 포기(수용 — 로그인 버튼 클릭 시 복구) / REFUTED 1건(AccountPanelViewModel 테스트 미작성 — 순수 이동+레포 관례상 비대상)은 미수정
  - **검증**: 빌드 경고 0·오류 0 / 테스트 116/116(신규 6: AllItemsFailed 이벤트·RepeatOne 즉시 정지·일시정지 중 에러 재개 시 스킵·StartLastAsync NotFound/재개 2종) / **HUMAN-VERIFY 잔여**: ① 트레이 메뉴 4항목 실동작(특히 종료) ② 홈·설정 로그인 버튼 아이콘 표시(공용 컨트롤 전환) ③ 플레이리스트 긴 제목 말줄임 ④ 홈 폭 1112 유지(코드비하인드 계산 전환) ⑤ 자막 곡 전환 유지
- 2026-07-17: **홈 로그인 버튼 구글 아이콘 + 프리미엄 안내 문구** — 사용자 요청(소형 UI, plan 없이 직접 수정). 홈 로그인/로그아웃 버튼에 구글 "G" 4색 벡터 아이콘(Wikimedia Commons 공식 SVG path 원본 — 에셋 파일 없이 XAML Path 4개, 브랜드 고정색이라 앱 토큰 비대상 주석 명시) + 버튼 아래 안내 캡션 `HomePremiumHint`(ko "유튜브 프리미엄에 가입된 계정으로 로그인하면 광고 없이 재생됩니다." / en 대응, resw x:Uid) 추가. 계정 영역을 세로 StackPanel(상태+버튼 행 / 캡션)로 재구성, 우측 정렬·MaxWidth 340 줄바꿈. 변경: HomePage.xaml, resw ko/en, README. 검증: 빌드 경고 0·오류 0, 테스트 111/111 — **아이콘 모양·문구 표시는 HUMAN-VERIFY**(빌드는 시각 결과를 증명하지 못함).
  - **후속(같은 날, 사용자 스크린샷 보고)**: ① 창 가로 축소 시 제목·로그인 겹침 — 원인은 제목(*)+로그인(Auto) 2열 배치. 로그인 영역을 제목 **위** 별도 행(세로 StackPanel)으로 재배치(사용자 확정 순서 — 겹침 원인 자체 제거) + `AppPageMinWidth` 토큰(720) 신설, 홈 Layout Grid에 MinWidth 적용, Root ScrollViewer에 가로 스크롤(HorizontalScrollMode=Enabled·Visibility=Auto) — 최소 폭 미만으로 줄이면 가로 스크롤바 표시 ② 프리미엄 안내 문구 2줄 → TextWrapping·MaxWidth 제거로 1줄 고정. 변경: HomePage.xaml, DesignTokens.xaml. 검증: 빌드 경고 0·테스트 111/111, 실표시 HUMAN-VERIFY.
  - **후속 2(같은 날, 사용자 지정)**: 화면 폭 통일 — 홈·설정·정보 콘텐츠 **1000px 고정 + 가운데 정렬**(좁은 창은 가로 스크롤), 플레이리스트 **최소 1000px**(가로 전용 ScrollViewer 래퍼 — 세로는 내부 ListView 유지). 토큰 정리: `AppPageContentWidth`(1000)·`AppPageFrameWidth`(1112 — 홈 Grid가 페이지 패딩 56×2를 소유해 콘텐츠 1000 유지용) 신설, `AppSettingsCardMaxWidth`(900)·`AppPageMinWidth`(720) 삭제(참조 전수 교체 — 잔존 0 grep). 변경: DesignTokens.xaml, HomePage·SettingsPage·AboutPage·PlaylistsPage.xaml. 검증: 빌드 경고 0·테스트 111/111, 실표시(폭·가운데 정렬·스크롤바) HUMAN-VERIFY.
  - **후속 3(같은 날, 사용자 요청)**: 홈 화면 카드 형식화 — URL 입력(+재생 중 pill 포함)·재생 모니터·빠른 재생 3개 섹션을 설정 화면과 동일한 `AppCardBorderStyle` 카드(Padding 24,22)로 감쌈. Grid 6행→5행(pill이 URL 카드 안으로), RowSpacing 36→12(카드 문법), 헤더 아래 여백은 Margin으로 유지. URL 입력의 MaxWidth 860 제거(카드 내부 전체 폭 사용). 변경: HomePage.xaml. 검증: 빌드 경고 0·테스트 111/111, 실표시 HUMAN-VERIFY.
  - **후속 4(같은 날, 사용자 요청)**: 설정 화면 계정 카드 로그인 버튼에도 홈과 동일한 구글 "G" 4색 아이콘 추가(마크업 복제 — 공통화는 3회 반복 문턱 미달로 보류). 변경: SettingsPage.xaml. 검증: 빌드 경고 0·테스트 111/111, 실표시 HUMAN-VERIFY.
- 2026-07-17: **기본값 변경 + 홈 URL 영속·자동 재생 예외 + 홈 로그인 상태 + 재생 실패 스킵 보강 + 트레이 재작성 (T1~T8)** — plan: `docs/plans/2026-07-17-home-defaults-tray.md`, 브랜치 `task/home-defaults-tray`
  - **무엇을**: ① T1 PRD 갱신 — FR-16 기본 맞춤·FR-19 기본 켜짐·FR-5 음소거 기본 켬·FR-8 자동 실행 신규 설치 기본 켬(+홈 재생 예외)·FR-9 트레이 토글 2항목·FR-15 홈 로그인 상태·FR-1 URL 저장/스킵 보강 ② T2 AppSettings 기본값 3종(FitMode=Contain·IsMuted=true·AutoPlayOnLaunch=true)+Normalize 폴백 Contain+manifest `uap5:StartupTask Enabled="true"` ③ T3 `AppSettings.LastHomeUrl`(additive) — PlayAsync 성공 시 기록·AttachCore에서 빈 입력란만 복원 ④ T4 `App.TryAutoPlayLastAsync` 초입에 "빠른 재생"(Loc 이름 비교) 생략 분기 — 일반 실행·부팅 공용 경로라 한 곳으로 둘 다 적용 ⑤ T5 `AccountPanelViewModel` 신설(SettingsViewModel 계정 로직 1:1 이동 — MonitorPanel 동형 공유 VM), 설정 카드 경로 위임 + 홈 제목 행 우측 상태·로그인/로그아웃 버튼(HomePage LoginWindow 열기 — SettingsPage 선례 복제), 페이지 재진입마다 상태 재확인 ⑥ T6 스킵 가드 분리 — `_errorAdvancePending`(중복 에러 무시, LoadAll 해제)+`_errorSkipCount`(Playing 리셋), 카운터≥큐 항목 수면 StopAsync+토스트(`Playback_AllItemsFailed` ko/en) ⑦ T7 트레이 재작성 — ContextMenuMode.SecondWindow→PopupMenu, 재생/정지·음소거 상태 연동 토글 2항목(StatusChanged/MutedChanged 구독+디스패처 마셜링+Dispose 해제, Opening 의존 제거), 트레이 재생 `StartAsync(id, LastItemId)` 재개(Deferred 재수용), resw Tray_Mute/Tray_Unmute 신설·Tray_Volume 삭제 ⑧ T8 README
  - **왜**: 사용자 요청 5건 — 기본값 지정(신규 설치만·질문 확정), 홈 URL 기억·홈 재생은 자동 재생 제외(부팅 포함·질문 확정), 홈 로그인 정보(여부만·질문 확정 — 계정명은 비공식 스크래핑이라 제외), 재생 실패 확인 중 발견한 스킵 공백(곡 시작 직후 에러 시 `_suppressEnded` 공유 가드로 스킵 미발화 — 기존 테스트는 Playing 후 에러만 검증) 수정, 트레이 스크롤·문구 미반영(H.NotifyIcon SecondWindow preview 버그 #21/#97 — PopupMenu 전환이 근본 해결·질문 확정)
  - **함정·결정**: 기본값 변경은 필드가 이미 기록된 기존 settings.json엔 미적용(신규 설치·파일 삭제 시만) / manifest Enabled=true는 업데이트 설치 시 기존 사용자 선택 유지(Windows 표준) / IsMuted 기본 반전으로 오디오 라우팅 테스트(mute:False 단언)가 깨져 Harness에 IsMuted=false 명시 고정 / 왕복 테스트 값은 기본값과 다르게 유지(직렬화 누락 감지) / 1곡 리스트 재생 불가는 첫 에러에서 즉시 정지(카운터 1≥1) / HomePage.xaml.cs에 `using DeskTube.Services;` 누락 시 C# CS0103이 XAML WMC0909/WMC9999 연쇄 오류로 위장(기지 패턴 재확인) / 트레이 문구는 Opening이 아니라 상태 이벤트 시점 선갱신(PopupMenu 모드 무관 견고)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 111/111(신규 6: LastHomeUrl 왕복·기본 null, 기본값 단언 3종 갱신, 에러 스킵 3종) / dotnet format 위반 0 / task별 spec+quality 이중 리뷰 — T5 spec MINOR 1(Detach 부재 — 무해, follow-up)·T6 quality MINOR 1(using 축약 — 즉시 반영) 외 전건 OK / plan-reviewer BLOCKER·MAJOR 0. **HUMAN-VERIFY 잔여**: ① 설정 파일 삭제 후 기본값(맞춤·음소거 켬·자동 재생 켬) ② 신규 설치 시 Windows 시작 앱 켬 ③ 홈 URL 재생→재시작 시 표시+자동 재생 안 됨 ④ 일반 리스트 재생→재시작 시 자동 재생 ⑤ 홈 로그인 상태·버튼 ⑥ 재생 불가 스킵·전곡 불가 정지 토스트 ⑦ 트레이 스크롤 없음·문구 전환(일시정지 중엔 "재생"만 — 트레이 직접 정지 불가는 토글 2항목 설계의 의도)
  - **미처리 Deferred**: AccountPanelViewModel Detach 부재(내부 구독 0 — 외부 이벤트 구독 도입 시 대칭 추가)
- 2026-07-17: **자막 표시 on/off 토글 (T1~T3, FR-20 신설)** — plan: `docs/plans/2026-07-17-captions-toggle.md`, 브랜치 `task/captions-toggle`
  - **무엇을**: ① T1 PRD FR-20 신설(자막 토글 — 켬=강제 표시/끔=강제 숨김, 기본 끔) + FR-10에 "자막" 추가 ② T2 명령 파이프라인 — `AppSettings.CaptionsEnabled`(기본 false), `IPlayerHost.SetCaptionsEnabled`(구현체 PlayerHost·FakePlayer 전수), `PlayerCommand.Enabled`(WhenWritingNull — 기존 페이로드 불변), `PlaybackCoordinator.SetCaptionsEnabledAsync`(전 플레이어 전파+저장, SetFitModeAsync 동형) + 플레이어 생성 3곳 초기 적용, player.html `captions` 명령 + `desiredCaptions`/`applyCaptions()`(loadModule/unloadModule 'captions'+'cc' 양쪽, 존재 확인+try/catch, load 시마다 재적용) ③ T3 설정 UI — SettingsViewModel.CaptionsEnabled + OnCaptionsEnabledChanged(Apply 경유), SettingsPage 화면 그룹 끝 CaptionsCard(ToggleSwitch TwoWay), resw ko/en 2키, README
  - **왜**: 사용자 요청 — 유튜브 계정의 자막 선호(쿠키) 때문에 배경 재생에 자막이 표시됨. IFrame API 공식 playerVars에 "강제 끄기"가 없어 모듈 load/unload(비공식 API)로 제어 — 사용자 사전 고지·동의, 실패 시 재생 무영향 방어. 기본값 끔·2상태 토글·언어 선택 제외는 질문 라운드 확정
  - **함정·결정**: `dotnet test`는 `-p:Platform=x64` 필수(미지정 시 MSIX AnyCPU 에러 — Deferred 대장 기지) / 자막 명령은 켬·끔 어느 상태든 항상 명시 전송(계정 선호를 덮어야 하므로 — 초기 적용 테스트가 `captions:False` 검증) / 곡 전환(loadVideoById) 시 자막 모듈 상태 리셋 가능성 대비 JS가 load 처리 시마다 재적용(D5 — desiredMuted 패턴 복제)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 108/108(신규 3: 시작 초기 적용·토글 전파+저장·영속 왕복) / T2·T3 spec+quality 리뷰 첫 판 전건 OK / plan-reviewer BLOCKER·MAJOR 0. **HUMAN-VERIFY 잔여**: ① 기본(끔)에서 자막 미표시 ② 토글 켬 → 즉시 표시 ③ 곡 전환 후 유지 ④ 재시작 후 설정 유지
- 2026-07-17: **일시 알림을 자동 소멸 토스트로 전환 — 전 화면 (T1~T3)** — plan: `docs/plans/2026-07-17-toast-notices.md`, 브랜치 `task/toast-notices`
  - **무엇을**: ① T1 `ToastService`(static Attach/Show — 디스패처 마셜링·미등록 무시) + MainWindow 하단 중앙 토스트 호스트(토큰 바+심각도 글리프 E73E/E7BA/E783/E946, DispatcherTimer 성공/정보 3초·오류/경고 5초, 연속 알림 교체+리셋, IsHitTestVisible=False) — 기존 NoticeBar·ShowNotice 제거(App 호출부 교체) ② T2 홈·설정 전환 — VM Notice 프로퍼티·OnPanelNoticeCleared 제거, `MonitorPanelViewModel.NoticeCleared` 이벤트·발화·구독 2곳 원자 제거, 페이지 InfoBar 2곳 제거 ③ T3 플레이리스트 전환 — Notice 3종·ShowNotice 제거, 알림 9지점 ToastService 직접 호출(NotifyLinkCopied/Failed 시그니처 불변). 상시 안내 3곳(화질 설명·자동 실행 상태·로그인 차단 안내)은 유지(사용자 확정)
  - **왜**: 사용자 요청 — X로 닫아야 남는 상단 InfoBar를 유튜브 뮤직식 자동 소멸 토스트로. VM 3벌 중복 Notice 인프라 제거가 근본 해결(D1). 위치 하단 중앙·시간 3/5초 확정
  - **함정·결정**: SettingsViewModel엔 InfoBarSeverity using이 원래 없었음(Severity 미사용 2종) — C# 컴파일 실패가 XAML 컴파일러 WMC9999/WMC1111 연쇄 오류로 위장돼 무관 파일(MonitorCardsControl)을 가리킴 / 토스트 글리프도 PUA 리터럴 저장(도구 계층이 \u 이스케이프를 문자로 변환 — 바이트 검증 필수) / ShowNotice 위임 함수는 남기지 않고 호출부 직접 전환(잉여 간접화 제거, 규칙 5-1)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 106/106 / Notice 계열 심볼 잔존 0(grep)·상시 안내 3곳 불변 / task별 spec+quality 리뷰(T1 spec MINOR 주석·T2 quality MINOR 빈 줄 — 즉시 반영, 나머지 OK). **HUMAN-VERIFY 잔여**: 복사 성공 토스트 3초 소멸, URL 오류 토스트 5초, 모니터 최소1 경고 토스트, 트레이 진입 안내 토스트, 연속 알림 교체, 상시 안내 3곳 그대로
- 2026-07-17: **플레이리스트 항목 공유 메뉴 + URL 복사 팝업 (T1~T2, FR-18 보강)** — plan: `docs/plans/2026-07-17-share-item-url.md`, 브랜치 `task/share-item-url`
  - **무엇을**: ① T1 항목 우클릭 메뉴 재구성(`위로/아래로 ─ 구분선 ─ 공유(E72D) ─ 구분선 ─ 삭제`) + ItemListView AttachedFlyout로 페이지 단일 공유 팝업(FlyoutPresenter 토큰 스타일 — 어두운 라운드 바, 읽기 전용 URL TextBox + 복사 버튼) + `OnShareClick`(ContainerFromItem 앵커, null 폴백 목록)·`OnShareCopyClick`(DataPackage/Clipboard, try/catch·AppLog) + VM `NotifyLinkCopied/Failed` public(ShowNotice 위임) + resw 4키(ko/en) ② T2 PRD FR-18 보강 + README
  - **왜**: 사용자 요청 — 유튜브 뮤직 공유 바 스타일 참고(픽셀 정합 아님), 공유 URL은 저장된 원본 그대로(D2), OS 공유 시트는 범위 외
  - **함정·결정**: Flyout을 Page.Resources에 두면 x:Name 필드 생성이 불확실 — ItemListView `FlyoutBase.AttachedFlyout` 트리 부착으로 해결 / sender(MenuFlyoutItem)는 행 시각 요소가 아니라 `ContainerFromItem`으로 앵커 획득(plan 리뷰 m1)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 106/106 / spec+quality 리뷰 첫 판 전건 OK. **HUMAN-VERIFY 잔여**: 메뉴 구조(구분선 2), 공유 팝업 표시·스타일, 복사 → 클립보드 값+성공 알림, 기존 이동/삭제 불변
- 2026-07-17: **작업 표시줄 미리보기 창 아이콘 수정 (버그)** — 증상: hover 썸네일 플라이아웃의 창 아이콘이 기본 아이콘(작업 표시줄 버튼은 정상). 원인: 미리보기는 패키지가 아니라 창(Win32) 아이콘을 쓰는데 WinUI 3 Window는 자동 설정 안 함 — 레포 전체 SetIcon 0건(grep 확정, 경량 단일 원인). 해결: MainWindow·LoginWindow 생성자에 `AppWindow.SetIcon("Assets/AppIcon.ico")`(Content 패키징 기확인 — 트레이 동일 파일). 검증: 빌드 경고 0·테스트 106/106·spec/quality 리뷰 OK, 플라이아웃 표시는 HUMAN-VERIFY(회귀 테스트 불가 — OS 셸). 브랜치 `task/fix-taskbar-preview-icon`.
- 2026-07-17: **한글 앱 이름 "데스크튜브" + 플레이리스트 마지막 선택 기억 (T1~T5, FR-18·NFR-4 보강)** — plan: `docs/plans/2026-07-17-appname-ko-lastselect.md`, 브랜치 `task/appname-ko-lastselect`
  - **무엇을**: ① T1 `AppSettings.LastSelectedPlaylistId`(Guid?, additive) + 왕복·구형 JSON 하위 호환 테스트 ② T2 PlaylistsViewModel — 비null 선택만 기록·fire-and-forget 저장, Populate 기본 선택(칩 예약 > 저장값 > 첫 리스트) ③ T3 resw `AppDisplayName`(ko 데스크튜브/en DeskTube) 신설 + manifest 표시명 3곳 `ms-resource:///Resources/AppDisplayName` 전환(StartupTask 선례 형식) + ko Tray_ToolTip·StartupTaskDisplayName·PrivacySummary 첫머리 데스크튜브화 ④ T4 창 제목·타이틀바·정보 화면 앱 카드 하드코딩 제거 → `Loc.Get("AppDisplayName")` 단일 키(언어 전환은 App.ApplyLanguageChange 창 재생성이 재배정 — 코드 확인) ⑤ T5 PRD FR-18·NFR-4 보강 + README
  - **왜**: 사용자 요청 3건 — 마지막 선택 기억(폴백 첫 리스트 확정), 홈 즉시 재생 반복(조사 결과 FR-7 기구현 — 코드 변경 없이 확인만으로 종결 확정), 앱 이름 한글화(전부 적용 확정, 개발자명 불변)
  - **함정·결정**: 마지막 "선택"은 기존 LastPlaylistId(마지막 "재생")와 별개 필드 — 재사용하면 재생 시마다 덮임 / null 선택(목록 재구성·삭제 중 일시 해제)은 저장 금지 / manifest ms-resource는 단축형 대신 레포 검증 선례 `ms-resource:///Resources/<키>`만 사용 / Package.appxmanifest는 BOM 유지(VS 생성 파일 관례 — hook 경고는 의도된 예외)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 106/106(신규: 왕복+구형 JSON) / task별 spec+quality 이중 리뷰(T1 spec MINOR 즉시 반영, T4 quality MAJOR stale 주석 1회 수정 후 2차 OK, 나머지 첫 판 OK). **HUMAN-VERIFY 잔여**: 시작메뉴 앱 이름(MSIX 재배포 후)·창 제목·타이틀바·정보 화면·트레이 툴팁 한글 표시, 언어 전환 직후 갱신, 플레이리스트 재진입·재시작 선택 복원(삭제 시 첫 리스트), 홈 즉시 재생 반복(FR-7 기구현 확인)
- 2026-07-17: **정보 화면 개인정보 처리방침 전문 안내 삭제** — 사용자 요청. AboutPage의 `PrivacyDocNote` TextBlock + ko/en resw 키 제거(요지 `PrivacySummary`는 유지, `docs/privacy-policy.md` 파일 자체는 보존). 검증: 잔존 참조 0건·빌드 경고 0. 화면 표시는 HUMAN-VERIFY.
- 2026-07-17: **소리 배지 리스타일 + 홈 배지 클릭 음소거 토글 (T1~T5, FR-5 보강)** — plan: `docs/plans/2026-07-17-audio-badge-restyle.md`, 브랜치 `task/audio-badge-restyle`
  - **무엇을**: ① T1 토큰 — `AppBadgeCornerRadius` 20→4(알약→사각), 배지 전경 `#1A1A1C`·음소거 배경 `#3A3A40`/전경 `#B8B8BE` 토큰 3쌍 + HC 사전 매핑 ② T2 `PlaybackCoordinator.MutedChanged` 공개 이벤트(additive — SetMutedAsync에서 상태 반영 후 발생, 값 정본은 Settings.IsMuted) + 단위 테스트 ③ T3 배지 식 변경 `ShowAudioBadge = 선택∧오디오 대상`(비음소거 조건 제거) + `MonitorChoice.IsAudioMuted` + `MonitorPanelViewModel.ToggleMuteCommand` + Attach/Detach에서 MutedChanged 구독/해제(디스패처 마셜링) + SettingsViewModel 중복 배지 갱신 제거 ④ T4 두 템플릿 배지 재작성 — 컬러 이모지 🔊 제거, FontIcon 글리프(E767 소리/E74F 음소거) + 상태별 토큰 브러시, 홈(Large) 배지만 Button화(`MuteToggleCommand` DP — HomePage만 바인딩, 설정은 Border 표시 전용), resw 키 교체(`MonitorAudioBadge` → `Monitor_AudioBadgeOn/Muted/ToggleName`) + 홈 힌트 문구 갱신 ⑤ T5 PRD FR-5 문구·검증 보강 + README
  - **왜**: 사용자 요청 — 배지 가독성(흰 글자 대비 3.3:1)·이모지 뭉개짐·모양 개선 + 홈에서 배지 클릭 음소거. 기존 "음소거 시 배지 숨김" 규칙은 클릭 해제 불가 모순 → 배지 상시 표시 + 소리/음소거 상태 구분으로 확정(질문 Q1·Q2, 코럴 배경+진한 글자 채택)
  - **함정·결정**: 배지 Button은 DataTemplate 안이라 DP에 x:Bind 직접 불가 — OnCardClick 동형 Click 핸들러로 DP 커맨드 위임 / 중첩 Button은 pointer 자체 처리로 카드 클릭과 분리(전파 관측 시 e.Handled 폴백 예정) / BadgeGlyph는 소스에 PUA 리터럴(E74F/E767)로 저장 — 에디터에서 빈 문자열처럼 보여도 정상(바이트 검증) / T1 prefilter가 플랫폼 미명시 빌드로 ESCALATE 오탐 → C 격상 처리
  - **검증**: 빌드 경고 0·오류 0 / 테스트 105/105(신규 1: MutedChanged 발생) / 옛 키 잔존 0건 / task별 spec+quality 이중 리뷰 전건 OK(T4 quality MINOR 1 — 접근성 이름 상태 미반영, follow-up 등록). **HUMAN-VERIFY 잔여**: 홈 배지 클릭 → 음소거 전환+배지 상태 전환+카드 선택 불변, 설정 배지 표시 전용, 트레이 토글 시 열린 화면 배지 즉시 갱신, 배지 모양·색 가독성
  - **Deferred 미처리분**: 설정 음소거 ToggleSwitch 실시간 동기화(MutedChanged 구독으로 해결 가능) / [SUGGEST] 배지 접근성 이름 상태 반영형(BadgeToggleName(bool))
- 2026-07-17: **"앱 시작 후 자동 재생" 토글 + 마지막 항목 재개 (T1~T5, FR-19 신설)** — plan: `docs/plans/2026-07-17-autoplay-on-launch.md`, 브랜치 `task/autoplay-on-launch`
  - **무엇을**: ① T1 PRD — FR-19 신설(앱 시작 후 자동 재생, Must)·FR-8 "마지막 항목부터" 보강·FR-10 항목 추가 ② T2 `AppSettings.LastItemId`·`AutoPlayOnLaunch` additive 필드 + `PlaybackCoordinator.LoadAll`(항목 시작 단일 경로, 호출부 3곳 전수)에서 LastItemId 기록 — StartAsync 기존 저장 편승·AdvanceAsync 저장 추가·ReloadCurrentTrack 동일 값 재설정 무해 ③ T3 `App.InitializeServicesAsync` 분기 `autoPlay || AutoPlayOnLaunch`(토글은 일반 실행 전용, 부팅은 FR-8로 항상) + `TryAutoPlayLastAsync`가 LastItemId를 `StartAsync(playlistId, startItemId)`(FR-18 인프라 재사용)로 전달 — 부팅·일반 공용이라 분기 없이 통일 ④ T4 설정 UI — AutoStart InfoBar와 언어 카드 사이 AutoPlayCard(SettingsCard+ToggleSwitch, 시안 문구 원문 고정), VM property+Populate+저장 핸들러(ApplyPausePolicy 패턴), resw ko/en 2키 ⑤ T5 README
  - **왜**: 사용자가 시안 스크린샷으로 설정 항목 누락 지적 — 직전 restyle plan에서 "기능 신설이라 PRD 합의 필요"로 Deferred했던 항목의 재수용. 질문 확정: 재개는 항목(곡) 단위(곡 처음부터, 초 단위 위치 아님) + 부팅 자동 재생도 마지막 항목부터 통일
  - **함정·결정**: LoadAll 호출부는 3곳(StartAsync·ReloadCurrentTrack·AdvanceAsync) — plan-reviewer가 초안의 "2곳" 오기를 BLOCKER로 잡음(ReloadCurrentTrack은 현재 곡 재로드라 무해) / 곡 전환마다 settings.json 저장(곡당 1회 — 미미) / 항목 삭제 시 PlaybackQueue.Start가 무시하고 리스트 기본 시작(기존 테스트 보장)
  - **검증**: 빌드 경고 0·오류 0 / 테스트 104/104(신규 2: 시작·진행 시 LastItemId 저장, 왕복·기본값 보강) / format 0 / task별 spec+quality 이중 리뷰(T2 spec MAJOR 1 — 왕복 Assert 누락 → 수정 후 OK, 나머지 전건 첫 판 OK). **HUMAN-VERIFY 잔여**: 토글 켬→앱 재실행 시 마지막 곡부터 재생, 토글 끔→재생 없음, 부팅 자동 시작 마지막 곡부터, 설정 카드 문구·위치
  - **변경 파일**: docs/prd.md, src/DeskTube/{Models/AppSettings.cs·Services/PlaybackCoordinator.cs·App.xaml.cs·ViewModels/SettingsViewModel.cs·Views/SettingsPage.xaml·Strings/{ko-KR,en-US}/Resources.resw}, tests/{PlaybackCoordinatorTests·JsonStateStoreTests}.cs, README.md
  - **미처리 Deferred**: 트레이 메뉴 "재생"도 마지막 항목부터 재개(일관성 개선 후보 — TrayIconService.PlayAsync는 항목 미지정)
- 2026-07-17: **트레이 아이콘 교체·재생 순서 설정 제거·반복 재생·라이선스 링크화 (T1~T6)** — plan: `docs/plans/2026-07-17-tray-repeat-license.md`, 브랜치 `task/tray-repeat-license`, PRD FR-7·10·12 갱신 연동
  - **무엇을**: ① T1 PRD 갱신(FR-7 전 모드 끝 반복 명시, FR-10 재생 모드 항목 제거, FR-12 라이선스 목록+클릭 시 공식 사이트 이동, 변경 이력 3건) ② T2 트레이 아이콘 — 루트 AppIcon.ico(16~256px 9엔트리)를 `Assets/AppIcon.ico`로 복사, TrayIconService URI·csproj Content 갱신, tray.ico 삭제(사전 승인) ③ T3 설정 "재생 순서" 카드+"재생" 그룹 헤더(유일 항목) 제거 — SettingsViewModel ModeIndex/ModeOptions/OnModeIndexChanged, resw 7키×2언어(ModeCard.Header·SettingsGroupPlayback.Text·Mode_* 5) 정리, AppSettings.Mode·SetModeAsync는 잔존(셔플듣기·저장 호환) ④ T4 반복 재생 — PlaybackQueue 순차 분기를 `(cur+1)%count` 순환으로(정지 경로는 빈 목록뿐), 재생 진입점(전체듣기/셔플듣기/행 재생)이 모드를 항상 명시(`shuffle ? Shuffle : Sequential` — 셔플 고착 방지, 사용자 확정), 주석 동기화 ⑤ T5 라이선스 — index.json에 nuspec projectUrl 5개, `LicenseEntry(Id,License,Url)` 전환(FullText 제거), AboutPage Expander→`SettingsCard IsClickEnabled`+OpenInNew 아이콘+`Launcher.LaunchUriAsync`(실패 시 로그), 전문 txt는 고지 의무로 배포 동봉 유지(사용자 확정), url 검증 테스트 신규 ⑥ T6 README 갱신
  - **왜**: 사용자 요청 4건 — 트레이 아이콘을 앱 아이콘과 통일 / 설정 단순화 / 재생 완료 시 멈추지 않고 처음부터 반복 / 라이선스 전문 대신 공식 사이트 링크
  - **함정·결정**: SettingsCard 클릭 속성은 `IsClickable`이 아니라 **`IsClickEnabled`**(계획 오기 — XamlCompiler WMC0011로 발견, 패키지 dll에서 확정) / 순차 반복으로 RepeatAll과 동작이 같아졌지만 enum은 저장 JSON 하위 호환 위해 잔존(D1·D7) / 설정 콤보 제거 후 "마지막 모드 유지"를 그대로 두면 셔플 고착 발생 — 진입점 모드 명시가 필수 동반 수정(D2)
  - **검증**: `dotnet build DeskTube.slnx -p:Platform=x64` 경고 0·오류 0 / `dotnet test -p:Platform=x64` 102/102(신규 2: 1곡 순차 반복·url 검증, 교체 2: 끝 정지→반복 단언) / `dotnet format` 위반 0 / task별 spec+quality 이중 리뷰 전건 OK. **HUMAN-VERIFY 잔여**: 트레이 아이콘 실표시(loose 배포는 Remove-AppxPackage 후 재등록 필요 — resources.pri 캐시 함정), 라이선스 클릭 시 브라우저 열림, 반복 재생 실청취
  - **변경 파일**: docs/prd.md, src/DeskTube/{Assets/AppIcon.ico(신규)·Assets/tray.ico(삭제)·Assets/licenses/index.json·DeskTube.csproj·Services/{TrayIconService·PlaybackQueue·PlaybackCoordinator}.cs·Models/PlaybackMode.cs·ViewModels/{SettingsViewModel·PlaylistsViewModel·AboutViewModel}.cs·Views/{SettingsPage·AboutPage}.xaml(+AboutPage.xaml.cs)·Strings/{ko-KR,en-US}/Resources.resw}, tests/{PlaybackQueueTests·PlaybackCoordinatorTests·LicenseInventoryTests}.cs, README.md
  - **미처리 Deferred**: "전체듣기 모드 명시"(PlaylistsViewModel 1줄) 단위테스트 — VM 테스트 인프라 부재로 미작성, 인프라 도입 시 재검토
- 2026-07-17: **URL 입력 플레이스홀더 세로 중앙 정렬 수정** — 홈·플레이리스트의 URL TextBox에서 플레이스홀더가 상단에 붙는 문제(사용자 스크린샷 보고). 원인: WinUI 1.8/2.x `DefaultTextBoxStyle` 템플릿은 `ContentElement`(ScrollViewer)·`PlaceholderTextContentPresenter`(TextBlock) 모두 `VerticalContentAlignment`를 템플릿 바인딩하지 않아(generic.xaml 확인) 해당 속성이 무효 — 세로 위치는 `Padding`만으로 결정됨. 수정: 무효한 `VerticalContentAlignment="Center"` 제거 후 세로 패딩으로 중앙 맞춤(HomePage h48 → `18,13`, PlaylistsPage h42 → `16,10` — (높이−테두리2−줄높이≈20)/2) + 이유 주석. 검증: `dotnet build src/DeskTube/DeskTube.csproj -p:Platform=x64` 경고 0·오류 0 — **실표시는 HUMAN-VERIFY**.
- 2026-07-17: **테스트 프로젝트 TFM net10 정렬** — 본체 csproj가 net10.0-windows10.0.22621.0으로 올라가 있어 net8 대상이던 `tests/DeskTube.Tests.csproj`가 NU1201로 솔루션 빌드를 깨던 문제. TargetFramework 1줄을 본체와 동일하게 변경. 검증: `dotnet build DeskTube.slnx -p:Platform=x64` 경고 0·오류 0 + `dotnet test -p:Platform=x64` 100/100 통과.
- 2026-07-17: **UI 전체 재설계 — Claude Design 시안(DeskTube 1a) 반영 + 다크 테마 고정** — plan: `docs/plans/2026-07-16-design-1a-restyle.md` (T1~T8, PRD 연결)
  - **무엇을**: 설정 창 4페이지(홈·플레이리스트·설정·정보)를 시안 DeskTube 1a(다크 #1A1A1C + 코럴 #F25C54)로 전면 재구성. ① T1 AGENTS.md 디자인 규칙 개정(시안 기준·다크 고정·토큰 의무 — AGENTS.md는 gitignore라 디스크만) ② T2 테마 변경 기능 제거(FR-17 REMOVED — App.xaml `RequestedTheme="Dark"` 고정, ThemeHelper.cs·AppTheme.cs 삭제, AppSettings.Theme 필드·설정 테마 카드·resw 테마 5키·테스트 2줄 제거) + `Resources/DesignTokens.xaml` 신설(D3 Color 35키+Brush 37종, HighContrast 사전, 치수 토큰 12종, 공용 Style 6종, NavigationView·ListView·SettingsCard lightweight 키) + SystemAccentColor 계열 코럴 재정의 ③ T3 셸(타이틀바 44px+아이콘, 사이드바 220px 고정 pane, 설정 일반 항목화·정보 footer, 콘텐츠 좌상단 라운드 카드, Mica 제거, 캡션 버튼 색 토큰 정합, `NavigateToPlaylists`·`App.Main` 신설) ④ T4 모니터 카드 공용화(`MonitorChoice` 분리·확장, `MonitorPanelViewModel`(열거·최소1개·저장·배지 — ResolveTargets/ResolveAudioTarget 재사용, WndProc 마셜링, Attach 멱등/Detach 대칭), `MonitorCardsControl`(Large 300×188/Compact 200×125, hover 코럴), SettingsViewModel 위임) ⑤ T5 홈(제목 "바탕화면에서 재생"+부제, 재생 중 pill+정지(StatusChanged 마셜링), 모니터 대형 카드, 빠른 재생 칩→플레이리스트 이동·선택(pending 소비)) ⑥ T6 플레이리스트(좌 300px·점선 새 리스트 버튼·활성 인디케이터(IsActive)·컬럼 헤더 제거·썸네일 64×40·hover 코럴 3쌍) ⑦ T7 설정(그룹 헤더 4개 화면/소리/재생/일반·순서 재배열·볼륨 값 라벨·콤보 문구 시안 원문화·화질 배너 코럴 틴트) ⑧ T8 정보(앱 카드 한 줄 정보·개인정보 카드·라이선스 카드형 Expander)
  - **왜**: 사용자가 Claude Design에서 만든 시안(공유 프로젝트 "WinUI3 앱 UI 전체 재설계")을 앱에 반영 요청. 기능은 기존 FR 유지(시각 재구성만), 단 후속 지시로 테마 변경 기능은 삭제(PRD FR-17 폐기·FR-10 갱신, 변경 이력 기록). 시안 확정 값은 plan의 `## 시각 요소 분해`·D3 표가 정본(스크래치패드 원본은 세션 종료 시 소실).
  - **어떻게(핵심 결정)**: 색·치수 전부 토큰화(AGENTS 규칙 2 — D3 키명 Color + 대응 Brush 이중 구조), 다크 accent 면이 SystemAccentColorLight2를 소비하므로 Light2=#F25C54 재정의, hover는 PointerEntered/Exited+IsEnabled 가드(x:Bind는 IsSelected 변경 때만 재평가), 텍스트는 램프 스타일+오버라이드(AGENTS 규칙 4), 활성 표시는 PlaylistEntry.IsActive+x:Bind 정적 함수, 홈 하단 정렬은 ScrollViewer MinHeight 기법.
  - **검증**: 매 task `dotnet build -p:Platform=x64` 경고 0·오류 0 + `dotnet test` 100/100 + `dotnet format` 위반 0 + spec/quality 이중 리뷰(지적 전건 수정 후 재리뷰 OK — T2 D3 키 정합 B1, T6 활성 인디케이터 B1 등). 삭제 심볼·미사용 resw 키 잔존 grep 0, View 색 하드코딩 0. **화면 실표시(색·간격·인디케이터·hover·다크 고정)는 ⏳ HUMAN-VERIFY** — 빌드는 시각 일치를 증명하지 못함.
  - **변경 파일**: MainWindow.xaml(.cs), App.xaml(.cs), Resources/DesignTokens.xaml(신규), Controls/MonitorCardsControl.xaml(.cs)(신규), ViewModels/{MonitorChoice(신규)·MonitorPanelViewModel(신규)·HomeViewModel·SettingsViewModel·PlaylistsViewModel·AboutViewModel}, Views/{HomePage·PlaylistsPage·SettingsPage·AboutPage}.xaml(.cs), Models/AppSettings.cs, Services/ThemeHelper.cs(삭제), Models/AppTheme.cs(삭제), Strings/{ko-KR,en-US}/Resources.resw, tests/JsonStateStoreTests.cs, docs/prd.md, AGENTS.md(로컬)
  - **미처리 Deferred**: "앱 시작 후 자동 재생" 토글(기능 신설 — PRD 합의 필요) / HighContrast 정밀 정합 / [SUGGEST] 램프 스타일 잔존 패턴 일괄 점검(T3~T6 화면) / [SUGGEST] ListView lightweight 3키 전역 스코프(향후 다른 화면 ListView 추가 시 유의) / 홈 pill 제목·썸네일(기존 대장 항목 유지)
- 2026-07-16: **설정 메뉴 클릭 크래시(c0000409) 조사 + WebView2 수명·예외 로깅 수정** — 조사 로그: `docs/plans/2026-07-16-debug-settings-crash.md`
  - **무엇을**: 사용자 보고(17:52 설정 클릭 시 fail-fast) 계기로 당일 크래시 6건 전수 분석(이벤트 로그·WER·크래시 덤프 4개 cdb/dotnet-dump 분석·UIA 자동 재현 12회+). 덤프 확정 원인 1건에 대해 승인 후 수정 2건 적용: ① LoginWindow — WebView2 이벤트 콜스택 안 직접 `Close()`를 `DispatcherQueue.TryEnqueue` 지연으로 교체 + `Closed`에서 `NavigationCompleted` 구독 해제·`LoginWebView.Close()` 확정 해제 ② App — 미처리 예외 로깅 핸들러 2종(XAML `UnhandledException` + `AppDomain.UnhandledException`, Handled 미설정·로그만)
  - **왜**: 덤프 2건(12:16·12:22)이 "WebView2 컨트롤러 생성 재시도 완료 콜백 → 이미 해제된 XAML WebView2 컨트롤(LoginWindow 전용) → AV → CLR FailFast"를 확정 — 창을 WebView2 이벤트 안에서 닫고 확정 해제가 없던 코드 결함과 부합. 예외 핸들러 부재로 크래시가 무기록 즉사해 조사가 어려웠던 문제도 해소(part2 이후 Deferred였던 "전역 예외 훅 상시 탑재" 이행).
  - **사용자 보고 건(17:52)의 판정**: 재생 중 설정 첫 진입에서 페이지 생성~값 채우기 구간 크래시(앱 로그 0줄 = Populate 로그 이전). 현 빌드에서 동일 조작(재생 중 포함) 재현 실패. 최유력 가설은 **stale loose 배포**(같은 날 음소거 세션이 기록한 resources.pri 캐시 함정과 교차 부합 — 18:42 재등록 직후 18:43부터 정상) — 소급 확정 불가로 미확정 처리. 12:05 건은 기지 함정(패키지 exe 직접 실행 0x80040154).
  - **검증**: `dotnet build DeskTube.slnx -p:Platform=x64` 및 `--no-incremental` 재빌드 경고 0·오류 0 / `dotnet test -p:Platform=x64` 100/100 / spec-compliance 리뷰 이슈 0 / quality 리뷰 BLOCKER 1(「XAML WebView2에 Close() 없음」 환각 API 의심) → **반증**: 해당 호출 포함 전체 재컴파일 통과(컴파일 = 존재 증명, 리뷰어가 다른 winmd 조회 오탐). 런타임 동작(로그인 창 여닫기 반복·크래시 재발 여부)은 ⏳ HUMAN-VERIFY.
  - **변경 파일**: `src/DeskTube/Views/LoginWindow.xaml.cs`, `src/DeskTube/App.xaml.cs`, `docs/plans/2026-07-16-debug-settings-crash.md`(신규)
  - **재발 방지 메모**: 원인 불명 크래시 재발 시 ① 앱 로그에서 "처리되지 않은 …예외" 확인(이번 수정으로 기록됨) ② 클린 재배포(Remove-AppxPackage 후 재등록 — LocalState 삭제 주의) 우선 시도.
## 아카이브 인덱스
- 2026-07.md: part1 코어 엔진·part2 UI 통합·배경창 Win32 호스트(AV 크래시 수정)·UI 성능 배치(FR-16 크기 모드 등)·음소거 토글 분리·멜론 차트 스타일(FR-18)
